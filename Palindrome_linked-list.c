// program to check if a linked list is palindrome
// I'm using a approach to check the original linked list with its clone list having elements in reversed order

#include <stdio.h>
#include <stdlib.h>

struct node
{
    int data;
    struct node *next;
};
typedef struct node *NODE;

int main(void)
{
    NODE head = NULL;
}